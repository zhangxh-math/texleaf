# TeXLeaf 1.2.1 visual editor migration

Goal: port the approved visual editor changes from TexLeaf-Z through f4d8813 into TeXLeaf, including quiver, with README/Wiki attribution, public README media, and passing Windows/macOS/Linux verification.

Base: TeXLeaf 36d5d12 already includes the visual changes through TexLeaf-Z f4e1fae. Use the existing isolated worktree and preserve TeXLeaf branding, GPL/NOTICE, CRLF coordinates, and LaTeX Workshop build/PDF/SyncTeX integration. Version: 1.2.1. Home, account, cloud, Git, and PDF library search are excluded.

- [x] Compare normalized shared files against the prior source base; port only visual core, math rendering, local preview, CodeMirror, protocol, Webview, and their tests. Preserve target-specific changes when the normalized base differs.
- [x] Adapt VisualEditorProvider table formula/source handling, quiver iframe and CSP, preview zoom, and toolbar actions without replacing LaTeX Workshop services. Add bridge-compatible positioning through the existing Workshop runtime bridge with public-command fallback.
- [x] Bundle pinned quiver 1.7.0, KaTeX 0.18.1, fonts, icons, quiver.sty and original licenses; port embedded editor code and build logic. Preserve source-version transaction checks and unsupported-option confirmation.
- [x] Run migrated regressions against the old implementation where practical, then type checks, complete serial tests, bundled worker checks and native visual scenarios (IME, Beamer, table, quiver, zoom and source round trips).
- [ ] Update README, actual GitHub Wiki, changelog, configuration docs, NOTICE/third-party attribution. State that q.uiver.app / varkor/quiver code is embedded, with exact version/commit and local modifications. Keep README GIFs/images at public HTTPS raw URLs and validate the packaged README/media.
- [ ] Push the migration branch, wait for all three OS jobs on the exact commit, resolve failures, then fast-forward main after checking it has not advanced. Synchronize Wiki, package 1.2.1, verify VSIX runtime/licenses/media and publish the GitHub release assets with SHA-256. Re-check public image URLs after push. Marketplace upload itself is not requested.

Local evidence: `.tmp/visual-121/full-check.log` (757/757 tests and bundle/worker smoke checks); `extension-host-final.log`; native quiver, table, IME, diagram color/zoom, Beamer typography/edit/undo/save, Workshop compiled references, ordinary Beamer and global-direct-frame SyncTeX all passed. Public CI and final packaged media checks remain release gates.
